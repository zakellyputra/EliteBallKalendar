FROM node:20-alpine

WORKDIR /app

# Install deps first for better caching
COPY package*.json ./
RUN npm install

# Copy app source
COPY . .

# Expose Vite + API
EXPOSE 3000 3001

CMD ["npm", "run", "dev"]
